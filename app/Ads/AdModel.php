<?php

namespace App\Ads;

/**
 * AdModel
 *
 * Represents a single ad record (maps to the `ads` table defined in
 * Section 5). Holds data/shape only — no query logic here, that
 * belongs in AdRepository.
 *
 * Stub only — properties added when the `ads` migration (5.1.d) lands.
 */
class AdModel
{
    //
}
