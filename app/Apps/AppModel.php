<?php

namespace App\Apps;

/**
 * AppModel
 *
 * Represents a single connected app record (maps to the `apps` table
 * defined in Section 5). Holds data/shape only — no query logic here.
 *
 * Stub only — properties added when the `apps` migration (5.1.b) lands.
 */
class AppModel
{
    //
}
