<?php

namespace App\Auth;

/**
 * UserModel
 *
 * Represents a single user record (maps to the `users` table defined
 * in Section 5), covering both advertiser and admin roles. Holds
 * data/shape only — no query logic here.
 *
 * Stub only — properties added when the `users` migration (5.1.a) lands.
 */
class UserModel
{
    //
}
