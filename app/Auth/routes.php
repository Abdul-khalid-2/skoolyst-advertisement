<?php

/**
 * Auth module routes.
 *
 * Merged into the main router at boot (see router boot code, 3.1.j).
 * Login, session, and API-key issuing endpoints are added here as
 * they're implemented (Section 6) — every path added must be
 * prefixed `/api/v1/` (Section 4 rule), same as Ads/Admin/Apps.
 */

return [];
